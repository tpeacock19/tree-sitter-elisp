================================================================================
Function definition
================================================================================

(defun foo (x &optional y)
  "stuff"
  x)
(defsubst bar () nil)

(cl-defun no-args () 123)

`(defun ,x () ,@body)

'(cl-defun 1)
'(cl-defun)
'(defun foo)

--------------------------------------------------------------------------------

(source
 (function_definition
  (defun_header (symbol)
    (symbol)
    (list
     (cons (symbol)
           (cons (symbol)
                 (cons (symbol))))))
  (string
   (string_fragment))
  (symbol))
 (function_definition
  (defun_header
    (symbol)
    (symbol)
    (list))
  (symbol (boolean)))
 (function_definition
  (defun_header (symbol)
    (symbol)
    (list))
  (number
   (integer)))
 (backquote
  (list
   (cons (symbol)
         (cons (backquote_eval (symbol))
               (cons (list)
                     (cons (splice (symbol))))))))
 (quote
  (list
   (cons (symbol)
         (cons (number (integer))))))
 (quote
  (list
   (cons (symbol))))
 (quote
  (list
   (cons (symbol)
         (cons (symbol))))))
